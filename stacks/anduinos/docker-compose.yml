version: '3.3'

services:
  docs:
    image: hub.aiursoft.cn/anduin/anduinos-docs
    networks:
      - proxy_app
    deploy:
      labels:
        swarmpit.service.deployment.autoredeploy: 'true'


networks:
  proxy_app:
    external: true