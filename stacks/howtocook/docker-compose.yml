version: '3.3'

services:
  app:
    image: hub.aiursoft.cn/ghcr.io/anduin2017/how-to-cook
    networks:
      - proxy_app
    deploy:
      replicas: 2
      labels:
        swarmpit.service.deployment.autoredeploy: 'true'

networks:
  proxy_app:
    external: true