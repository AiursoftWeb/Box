version: '3.3'

services:
  sites:
    image: hub.aiursoft.cn/aiursoft/sites
    network_mode: "host"
    volumes:
      - basic-data:/data
      - /etc/hostname:/etc/hostname
      - /etc/hosts:/etc/hosts
    deploy:
      labels:
        swarmpit.service.deployment.autoredeploy: 'true'

volumes:
  basic-data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /swarm-vol/basic-data
