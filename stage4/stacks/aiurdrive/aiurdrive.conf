# JS Games
drive.aiursoft.com {
    log
    import hsts
    import rate_limit
    encode br gzip
    reverse_proxy http://aiurdrive_app:5000 {
    }
}

drive.aiursoft.cn {
    log
    import hsts
    import rate_limit
    encode br gzip

    redir https://drive.aiursoft.com{uri} permanent
}