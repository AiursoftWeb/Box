# Untrusted.
tts.aiursoft.com {
    log
    import hsts
    import limit_to_cloudflare
    encode br gzip
    reverse_proxy http://reporter_tts:8000 {
    }
}

# Untrusted.
reporter.aiursoft.com {
    log
    import hsts
    import limit_to_cloudflare
    encode br gzip
    reverse_proxy http://reporter_portal:5000 {
    }
}
