# Untrusted.
tts.aiursoft.cn {
    log
    import hsts
    import rate_limit
    encode br gzip
    reverse_proxy http://reporter_tts:8000 {
    }
}

# Untrusted.
reporter.aiursoft.cn {
    log
    import hsts
    import rate_limit
    encode br gzip
    reverse_proxy http://reporter_portal:5000 {
    }
}
