# Untrusted.
tts.aiursoft.com {
    log
    import hsts
    encode br gzip
    reverse_proxy http://reporter_tts:8000 {
    }
}

# Untrusted.
reporter.aiursoft.com {
    log
    import hsts
    encode br gzip
    reverse_proxy http://reporter_portal:5000 {
    }
}
