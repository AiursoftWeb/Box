# Untrusted.
vault.aiursoft.com {
    log
    import hsts
    import rate_limit
    encode br gzip
    reverse_proxy http://vaultwarden_app:80 {
    }
}

vault.aiursoft.cn {
    log
    import hsts
    import rate_limit
    encode br gzip
    redir https://vault.aiursoft.com{uri} permanent
}