kahla.aiursoft.com {
    log
    import hsts
    import limit_to_cloudflare
    encode br gzip
    reverse_proxy http://kahla_app:5000 {
    }
}

kahla.aiursoft.cn {
    log
    import hsts
    import limit_to_cloudflare
    encode br gzip
    redir https://kahla.aiursoft.com{uri} permanent
}