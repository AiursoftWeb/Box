# Untrusted
chess.aiursoft.com {
    log
    import hsts
    import limit_to_cloudflare
    encode br gzip
    reverse_proxy http://chess_app:5000 {
    }
}

# Untrusted
chess.aiursoft.cn {
    log
    import hsts
    import limit_to_cloudflare
    encode br gzip
    redir https://chess.aiursoft.com{uri} permanent
}
