# Protected by 2FA.
gitlab.aiursoft.cn {
    log
    import hsts
    import limit_to_cloudflare
    encode br gzip

    @upgrade_script {
        path /anduin/anduinos/-/raw/*/src/upgrade.sh
    }

    handle @upgrade_script {
        reverse_proxy http://gitlab_gitlab {
        }
    }

    handle {
        redir https://gitlab.aiursoft.com{uri} permanent
    }
}

# Protected by 2FA.
gitlab.aiursoft.com {
    log
    import hsts
    import limit_to_cloudflare
    encode br gzip
    reverse_proxy http://gitlab_gitlab {
    }
}
