# Untrusted.
nuget.aiursoft.com {
    log
    import hsts
    import limit_to_cloudflare
    encode br gzip
    reverse_proxy http://nuget_app:5000 {
    }
}

# Untrusted.
nuget.aiursoft.cn {
    log
    import hsts
    import limit_to_cloudflare
    encode br gzip
    redir https://nuget.aiursoft.com{uri} permanent
}

