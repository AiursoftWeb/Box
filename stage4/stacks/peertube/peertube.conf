# Protected by IP.

videos.aiursoft.com {
    log
    import hsts
    encode br gzip

    reverse_proxy http://peertube_peertube:9000 {
    }
}

videos.aiursoft.cn {
    log
    import hsts
    encode br gzip
    redir https://videos.aiursoft.com{uri} permanent
}