# Protected by IP.

videos.aiursoft.com {
    log
    import hsts
    import limit_to_cloudflare
    encode br gzip

    reverse_proxy http://peertube_peertube:9000 {
    }
}

videos.aiursoft.cn {
    log
    import hsts
    import limit_to_cloudflare
    encode br gzip
    redir https://videos.aiursoft.com{uri} permanent
}