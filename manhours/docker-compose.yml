version: '3.3'

services:
  manhours:
    image: hub.aiursoft.cn/aiursoft/manhours:latest
    volumes:
      - manhours-data:/data
    ports:
      - 48467:5000

volumes:
  manhours-data:
    driver: nfs
    driver_opts:
      share: 172.16.1.1:/mnt/docker/manhours