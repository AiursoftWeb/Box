apiVersion: 1

deleteDatasources:
  - name: Default-Prometheus
    orgId: 1

datasources:
- name: Default-Prometheus
  type: prometheus
  url: http://prometheus:9090
  access: proxy
  isDefault: true

  