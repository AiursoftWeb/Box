stages:
  - build

lint:
  stage: build
  before_script:
    - sudo apt-get update && sudo apt-get install -y python3-pip # Install pip first
    - pip3 install PyYAML                              # Then use it
  script:
    - python3 ./lint.py ./stage4/
