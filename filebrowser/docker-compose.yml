version: '3.3'

services:
  web:
    image: hub.aiursoft.cn/filebrowser/filebrowser
    networks:
      - proxy_app
    volumes:
      - files-data:/srv
      - /swarm-vol/filebrowser/database:/database.db
    environment:
      - FB_AUTHMETHOD=noauth
      - FB_NOAUTH=true
    deploy:
      labels:
        swarmpit.service.deployment.autoredeploy: 'true'

networks:
  proxy_app:
    external: true

volumes:
  files-data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /swarm-vol/filebrowser/data
